<!doctype html>
<html lang="es" data-bs-theme="light">

<head>
  <meta charset="utf-8" />
  <title>Ubicaciones</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/img/iconosanrafa.png" /> <!-- icono -->
  <style>
    /* Tamaño base un poco más pequeño */
    body {
      padding: 0;
      font-size: 0.9rem;
      background-color: #E8F4FF !important;
      /* color home */
      color: #0A3D62;
      /* color de texto home */
    }

    /* Nav igual que home */
    .navbar {
      background-color: #ecf6ff !important;
    }

    .navbar .nav-link {
      color: #0A3D62 !important;
    }

    .navbar .nav-link:hover {
      color: #562585 !important;
    }

    /* Contenedor más ancho y centrado */
    .page-wrapper {
      max-width: 1400px;
      margin: 0 auto;
    }

    /* Header local */
    header.page-header {
      margin-bottom: 1rem;
    }

    header.page-header h1 {
      font-size: 1.15rem;
      color: #0A3D62;
      /* color home */
    }

    header.page-header p {
      font-size: 0.82rem;
      color: #0A3D62;
    }

    /* Botón compacto reutilizable */
    .btn-compact {
      padding: 0.3rem 0.7rem;
      font-size: 0.8rem;
      line-height: 1.2;
      background-color: #562585 !important;
      /* botón morado home */
      border-color: #562585 !important;
      color: #fff !important;
    }

    .table thead th {
      white-space: nowrap;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: .03em;
      background-color: #ecf6ff;
      /* celeste suave home */
      color: #0A3D62;
    }

    .table {
      --bs-table-cell-padding-y: 0.4rem;
      --bs-table-cell-padding-x: 0.5rem;
      background-color: #ffffff !important;
      /* coherencia home */
    }

    .table td {
      vertical-align: middle;
      font-size: 0.85rem;
      color: #0A3D62;
    }

    /* Card cuadrada y fina */
    .table-card {
      border-radius: 0.375rem;
      background-color: #ffffff !important;
      border: 1px solid #CFE6FF;
      /* borde de home */
      box-shadow: 0 0.1rem 0.35rem rgba(15, 23, 42, 0.05);
      overflow: hidden;
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .table-card:hover {
      transform: scale(1.03);
      border: 2px solid #a3eef3 !important;
      box-shadow: 12px 6px 15px rgba(59, 145, 148, 0.4);
    }

    /* Hover tabla */
    .table tbody tr:hover {
      background-color: #f0f8ff;
    }

    /* Acciones centradas y compactas */
    .col-acciones {
      text-align: center;
      white-space: nowrap;
      width: 160px;
    }

    .col-acciones .btn {
      margin-bottom: 0.1rem;
      background-color: #5dc2bd !important;
      /* botón celeste home */
      border-color: #5dc2bd !important;
      color: #fff !important;
    }

    /* Modo oscuro compatible con home */
    body.dark-mode {
      background-color: #1e1e1e !important;
      color: #e6e6e6 !important;
    }

    main.dark-mode-main {
      background-color: #2a2a2a !important;
      color: #e6e6e6 !important;

      h1 {
        color: #f1f1f1 !important;
      }

      p {
        color: #f1f1f1 !important;
      }
    }

    /* letras */
    .dark-mode .card {
      background-color: #2a2a2a !important;
      color: #ffffff !important;
      border: 1px solid #444 !important;
    }

    .dark-mode .navbar,
    xxxxxx .dark-mode footer {
      background-color: #2a2a2a !important;
      color: #e6e6e6 !important;
    }

    .dark-mode a,
    .dark-mode .nav-link {
      color: #ffffff !important;
    }

    .dark-mode footer {
      background-color: #2a2a2a !important;
      color: #e6e6e6 !important;
    }
  </style>
</head>

<body class="bg-white d-flex flex-column min-vh-100">

  <!-- NAVBAR SUPERIOR (igual que en equipos.ejs) -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <div class="container">

      <!-- Logo + Nombre -->
      <a class="navbar-brand d-flex align-items-center gap-2" href="/">
        <img src="/img/logosanRafa.png" alt="USTA" style="height:40px; width:auto;" class="rounded">
      </a>

      <!-- Botón colapsable móvil -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
        aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Contenido navbar -->
      <div class="collapse navbar-collapse" id="mainNavbar">

        <!-- Ítems del menú -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/equipos">Equipos</a></li>
          <li class="nav-item"><a class="nav-link active" aria-current="page" href="/ubicaciones">Ubicaciones</a></li>
          <li class="nav-item"><a class="nav-link" href="/responsables-custodios">Responsables</a></li>
          <li class="nav-item"><a class="nav-link" href="/personas-mantenimiento">Personas</a></li>
          <li class="nav-item"><a class="nav-link" href="/cargos">Cargos</a></li>
          <li class="nav-item"><a class="nav-link" href="/mantenimientos">Mantenimientos</a></li>
          <!----Wendy------------------------------------------------->
          <% if (currentUser && currentUser.role==='admin' ) { %>
            <li class="nav-item">
              <a class="nav-link" href="/usuarios">Usuarios</a>
            </li>
            <% } %>
              <li class="nav-item"><a class="nav-link " href="/mi-perfil">Mi perfil</a></li>
        </ul>
        <!------------------------------------------------>

        <!-- Usuario + Salir -->
        <% if (currentUser) { %>
          <span class="navbar-text me-3 text-muted small">
            Usuario: <strong>
              <%= currentUser.username %>
            </strong> |
            Rol: <strong>
              <%= currentUser.role %>
            </strong>
          </span>

          <button id="toggleDark" class="btn btn-sm" style="border: none;">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-moon"
              viewBox="0 0 16 16">
              <path
                d="M6 .278a.77.77 0 0 1 .08.858 7.2 7.2 0 0 0-.878 3.46c0 4.021 3.278 7.277 7.318 7.277q.792-.001 1.533-.16a.79.79 0 0 1 .81.316.73.73 0 0 1-.031.893A8.35 8.35 0 0 1 8.344 16C3.734 16 0 12.286 0 7.71 0 4.266 2.114 1.312 5.124.06A.75.75 0 0 1 6 .278M4.858 1.311A7.27 7.27 0 0 0 1.025 7.71c0 4.02 3.279 7.276 7.319 7.276a7.32 7.32 0 0 0 5.205-2.162q-.506.063-1.029.063c-4.61 0-8.343-3.714-8.343-8.29 0-1.167.242-2.278.681-3.286" />
            </svg>
          </button>


          <form method="post" action="/logout" class="d-inline m-0">
            <button class="btn btn-outline-danger btn-sm">Salir</button>
          </form>
          <% } %>

      </div>
    </div>
  </nav>

  <!-- CONTENIDO PRINCIPAL -->
  <main class="flex-grow-1">
    <div class="container-fluid py-3 px-4">
      <div class="page-wrapper">

        <!-- Header local (título + descripción + botón Nueva) -->
        <header class="page-header d-flex justify-content-between align-items-center">
          <div>
            <h1 class="h4 m-0">Ubicaciones</h1>
            <p class="text-muted mb-0">
              Gestión de sedes, edificios, pisos y salas donde se encuentran los equipos.
            </p>
          </div>
          <div>
             <a href="/reportes/localidades.xlsx" class="btn btn-outline-success btn-compact">
              <i class="bi bi-file-earmark-excel"></i> Exportar Excel
              </a>
            <button id="btnNueva" class="btn btn-primary btn-compact">➕ Nueva ubicación</button>

            <!----------Wendy----------------------------------------------->
            <% if (currentUser && currentUser.role==='admin' ) { %>
              <button id="btnNueva" class="btn btn-primary btn-compact">➕ Nueva ubicación</button>
              <% } %>
                <!--------------------------->
          </div>
        </header>

        <!-- Filtro de búsqueda Hisaaa -------->
        <div class="card mb-3">
          <div class="card-body toolbar">
            <div class="row g-2 align-items-end">
              <div class="col-sm-8 col-md-6 col-lg-4">
                <label class="form-label">Buscar</label>
                <input type="text" class="form-control" id="search" placeholder="Identificacion o Edificio...">
              </div>
              <div class="col-sm-4 col-md-3 col-lg-2 d-flex gap-2">
                <button id="btnLimpiarFiltro" class="btn btn-outline-secondary btn-compact w-100">
                  Limpiar filtro
                </button>
              </div>
            </div>
          </div>
        </div>
        <!----------------->


        <!-- Tabla dentro de card sutil Hisaa -------------->
        <div class="card table-card">
          <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle mb-0">
              <thead class="table-light">
                <tr>
                  <th>#</th>
                  <th>Identificación</th>
                  <th>Sede</th>
                  <th>Edificio</th>
                  <th>Piso</th>
                  <th>Sala</th>
                  <th>Creado</th>
                  <th>Actualizado</th>
                  <th class="col-acciones">Acciones</th>
                </tr>
              </thead>
              <tbody id="tbody">
                <% if (ubicaciones && ubicaciones.length) { %>
                  <% ubicaciones.forEach(u=> { %>
                    <tr>
                      <td>
                        <%= u.id %>
                      </td>
                      <td class="fw-semibold">
                        <%= u.identificacion %>
                      </td>
                      <td>
                        <%= u.sede %>
                      </td>
                      <td>
                        <%= u.edificio %>
                      </td>
                      <td>
                        <%= u.piso %>
                      </td>
                      <td>
                        <%= u.sala %>
                      </td>
                      <td>
                        <%= u.createdAt ? new Date(u.createdAt).toLocaleString('es-CO', { timeZone:'America/Bogota' })
                          : '' %>
                      </td>
                      <td>
                        <%= u.updatedAt ? new Date(u.updatedAt).toLocaleString('es-CO', { timeZone:'America/Bogota' })
                          : '' %>
                      </td>
                      <td class="col-acciones">

                        <% if (currentUser.role==='admin' ) { %>
                          <button class="btn btn-warning btn-compact me-1" data-act="edit" data-id="<%= u.id %>">
                            Editar
                          </button>

                          
                          <button class="btn btn-danger btn-compact" data-act="del" data-id="<%= u.id %>">
                            Eliminar
                          </button>

                          <% } else { %>
                            <span class="text-muted small">Solo lectura</span>
                            <% } %>
                      </td>
                    </tr>
                    <% }) %>
                      <% } else { %>
                        <tr>
                          <td colspan="9" class="text-center text-muted py-4">Sin registros</td>
                        </tr>
                        <% } %>
              </tbody>
            </table>
          </div>
        </div>

        <!-- Modal -->
        <div class="modal fade" id="modal" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <form class="modal-content" id="form">
              <div class="modal-header">
                <h5 class="modal-title" id="title">Nueva ubicación</h5>
                <button class="btn-close" data-bs-dismiss="modal" type="button"></button>
              </div>
              <div class="modal-body">
                <input type="hidden" id="id">
                <div class="mb-3">
                  <label class="form-label">Identificación</label>
                  <input id="identificacion" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Sede</label>
                  <input id="sede" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Edificio</label>
                  <input id="edificio" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Piso</label>
                  <input id="piso" class="form-control" required>
                </div>
                <div class="mb-3">
                  <label class="form-label">Sala</label>
                  <input id="sala" class="form-control" required>
                </div>
              </div>
              <div class="modal-footer">
                <button class="btn btn-secondary btn-compact" data-bs-dismiss="modal" type="button">Cancelar</button>
                <button class="btn btn-primary btn-compact" type="submit">Guardar</button>
              </div>
            </form>
          </div>
        </div>

        <div class="toast-container position-fixed bottom-0 end-0 p-3" id="toastContainer"></div>

      </div><!-- /.page-wrapper -->
    </div><!-- /.container-fluid -->
  </main>

  <!------------Wendy--------------------->
  <script>
    window.CURRENT_USER_ID = "<%- JSON.stringify(currentUser ? currentUser.id : null) %>";

    document.addEventListener('DOMContentLoaded', () => {
      const search = document.getElementById('search');
      const btnLimpiar = document.getElementById('btnLimpiarFiltro');

      if (btnLimpiar && search) {
        btnLimpiar.addEventListener('click', () => {
          search.value = '';
          // Disparamos un evento input para que el JS que filtra se ejecute de nuevo
          const ev = new Event('input', { bubbles: true });
          search.dispatchEvent(ev);
        });
      }
    });
  </script>
  <!----------------------------------------------------->

  <!-- FOOTER "FIJO" ABAJO -->
  <footer class="mt-auto py-2 border-top bg-light text-center text-body-secondary small">
    © <%= new Date().getFullYear() %> USTA – Módulo de gestión de inventario y registro de mantenimientos de equipos
      informáticos HSRT
  </footer>
  <!---------------wendy--------------------------->
  <script>
    window.CURRENT_USER_ROLE = "<%- currentUser ? currentUser.role : '' %>";
  </script>
  <!------------------------------------------->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/ubicaciones.js"></script>

  <script>
    const toggle = document.getElementById("toggleDark");
    const main = document.querySelector("main");

    toggle.addEventListener("click", () => {
      document.body.classList.toggle("dark-mode");
      main.classList.toggle("dark-mode-main");

      toggle.textContent =
        document.body.classList.contains("dark-mode")
          ? "☀️"
          : "🌙";
    });
  </script>

</body>

</html>